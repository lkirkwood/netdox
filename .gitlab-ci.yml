---
image: "rust:latest"

services:
  - redis:latest

debug:
  stage: .pre
  script:
    - rustc --version
    - cargo --version

test:
  stage: test
  script:
    - >-
      cat functions.lua |
      sed 's/"/\'/g' |
      awk 'ORS=""; NR == 1 {print "FUNCTION LOAD REPLACE \""}
      $1 !~ /^--|^$/ {print $0 "\\\\n"} END {print "\"\n"}' |
      nc -N redis 6379
    - cargo test --verbose

lint:
  stage: test
  script:
    - cargo fmt --check
    - cargo clippy -- -D warnings
